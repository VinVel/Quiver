# Minor Patch

# Changes
- Update Dependencies

## Fixes
- Fix messed up linux packaging

## Install

- Windows and macOS users can download Quiver from the latest GitHub release.
- macOS users may need to remove the quarantine attribute because the `.dmg` is not signed or notarized.
- macOS users need Homebrew `ffmpeg`: `brew install ffmpeg`.
- Linux users can install the `.deb` or `.rpm` package from the release.

## Notes

- Regular Windows release builds are intended to work without a separate `Python`, `yt-dlp`, `Deno`, POT provider, or `FFmpeg` install.